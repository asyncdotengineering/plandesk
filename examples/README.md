# Plan Desk examples

`checkout-revamp.json` is a hand-authored `plandesk-export-v1` dogfood project: a checkout revamp plan with labeled dependency edges, linked scope/design docs, and an agent run — the same shape agents use on real plans. Import it with:

```bash
plandesk import --in examples/checkout-revamp.json
```
