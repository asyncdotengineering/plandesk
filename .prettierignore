node_modules/
dist/
.turbo/
coverage/
.handoff/
sprints/
pnpm-lock.yaml
**/routeTree.gen.ts
apps/docs/.astro/
apps/docs/dist/
*.tsbuildinfo

# Generated by drizzle-kit — snapshots + journal, not hand-edited.
packages/plandesk-db/drizzle/meta/

# Byte-identity-coupled to CLI template constants (curator-templates.ts,
# skill-template.ts, factory.ts). The *.test.ts drift guards assert these
# on-disk files are byte-for-byte equal to the embedded constants, so
# reformatting one side alone breaks the build. Format source + constant
# together, or leave both as-is — never let prettier touch only one.
.agents/
.plandesk/skill.md
