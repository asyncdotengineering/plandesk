{
  "name": "@plandesk/runner",
  "version": "0.1.0",
  "type": "module",
  "description": "Plan Desk runner — the machine-side agent that polls a Plan Desk board and executes dispatched work: config loading, worker resolution (declare-then-probe), per-attempt worktrees, headless worker dispatch, and the poll-claim-dispatch-settle loop gated by each task's validation command.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "plandesk-runner": "./bin/plandesk-runner"
  },
  "files": [
    "dist",
    "bin",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts",
    "!dist/**/*.map",
    "scripts"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "e2e": "node scripts/e2e.mjs"
  },
  "dependencies": {
    "smol-toml": "^1.6.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "25.9.2",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6"
  },
  "license": "MIT",
  "author": "asyncdotengineering",
  "homepage": "https://plandesk.asyncdot.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/asyncdotengineering/plandesk.git",
    "directory": "packages/plandesk-runner"
  },
  "keywords": [
    "plan-desk",
    "runner",
    "agent",
    "factory"
  ]
}
