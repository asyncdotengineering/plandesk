S4-03 Agent-runs panel — verification (2026-06-08)

API (automated):
1. GET /api/v1/projects/:id/agent-runs → 200 [{id, project_id, status, label, started_at, completed_at, events:[{id, message, created_at}]}]
2. Runs sorted newest-first; nested events omit run_id
3. Missing project → 404 {error:"not_found"}
4. agentRunService.start/recordProgress/complete emit agent_run_* SSE (existing S2-02)

Web (component + hook tests):
1. useSseInvalidation: agent_run_progress → invalidates queryKeys.agentRuns(projectId)
2. AgentRunsPanel: fetches agent-runs; renders label, status (Running/Completed/Failed), progress messages
3. Flow route mounts panel over canvas (top-left corner)

Live loop (service + SSE path):
1. start(projectId, label) → panel refetch shows new run with Running status
2. recordProgress(runId, message) → SSE invalidates → progress line appears without reload
3. complete(runId, status) → status updates to Completed/Failed

REQ-9 satisfied: agent_run_* SSE invalidates agentRuns query for sub-500ms panel refresh.
C16 satisfied: start run + progress events visible on canvas Agents activity panel.
