node_modules/
dist/
*.db
.plandesk/*.pre-*
.turbo/
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.plandesk/token
.plandesk/*.log
.handoff/result-*.txt
*.local

# Internal build/process artifacts — not part of the published product
.handoff/
sprints/
plandesk-manager-notes.md
# Per-session standdown handoff, rewritten every run — never a tracked file.
.plandesk/standdown.md
*-manager-notes.md
*-implementation-notes.md
*-scratchpad.md

# wrangler local cache/state (Cloudflare Workers dev)
.wrangler/

# bundled SPA (build artifact, generated at pack time)
packages/plandesk-api/web/
.plandesk/server.json

# server config file (plandesk.server.json) can hold secrets — never commit it
plandesk.server.json

# local secrets — never commit (GitHub OAuth, DB tokens, wrangler dev vars)
.env
.env.*
.dev.vars
.dev.vars.*

# transient dispatch state (briefs, logs, results) — factory.md
# metrics.jsonl is the exception: it is the routing/lane evidence and is tracked
runs/*
!runs/metrics.jsonl
.handoff/
scratchpad/
.plandesk/artifact-pushes.json
